#include <stdlib.h>
#include "qmem.h"
#include <errno.h>

int qmem_alloc(unsigned num_bytes, void ** rslt){
  *rslt = malloc(num_bytes);
  *rslt = z;
  if(*rslt == NULL)
    return -1;
  else if(errno != 0){
    //Catch the rest of the errors
    return -2;
  }
  return 0;
}
